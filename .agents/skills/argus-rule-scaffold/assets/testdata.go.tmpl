package {{SHORT_ID}}

import (
	"context"
)

func TestCases(ctx context.Context) {
	// 1. Negative Test: Pelanggaran nyata wajib dianotasi // want `pattern`
	UnsafeAction() // want `\[{{RULE_CODE}}\] Violation description.*`

	// 2. Positive Test: Kode compliant tanpa diagnostik (0 diagnostics)
	SafeCompliantAction()

	// 3. Whitelist Exception: Idiom valid yang aman dari false positive (0 diagnostics)
	ExemptAction()

	// 4. Directive Suppression: Wajib menyertakan alasan minimal 2 kata (0 diagnostics)
	// argus:ignore-{{SHORT_ID}} intentional verified exception in test suite
	UnsafeAction()
}
